# 这个文件告诉 git：「哪些东西不要存进版本记录」
# 作用：避免把一堆临时文件、依赖包塞进存档，让历史保持干净、可读

# 依赖包目录（本项目禁止使用 npm，这里只是防止意外生成）
node_modules/

# 系统自动生成的垃圾文件
Thumbs.db
Desktop.ini
.DS_Store

# 临时文件和日志
*.tmp
*.log
*.bak
~$*

# 编辑器/工具的工作目录
.vscode/
.idea/

# 注意：work/ 和 outputs/ 里的内容【要】存档，不要忽略。
# 因为「回退」的时候，要能退回到某一个具体的成品版本。

# 参照物的源码副本（是别人仓库的克隆，不是本项目的代码，不要存进来）
work/math-reader-boox-analysis/

# 临时的命令输出与一次性脚本（我用来把终端输出捞出来看 / 做一次性操作）
# 约定：以 _ 开头的都是「用完就删」，不属于项目资产
work/_*

# 防撞锁：同时只允许一个循环在跑，这个文件是运行时产物
work/.loop.lock

# 我们自己叫起来的那个 Codex 的进程号（用来认领孤儿进程），运行时产物
work/.ours_codex.pid

# Codex 每一轮的原始输出可能很长，留在磁盘上排查用，但不进版本记录
# （同一轮的「派活提示词」和「最后回复」要存，那两个有长期价值）
logs/轮次_*_codex输出.txt
logs/轮次_*_codex事件.jsonl


# Python 运行时自动生成的缓存（不是源码，不要存）
__pycache__/
*.pyc
