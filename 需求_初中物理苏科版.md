# 需求交接 · 初中物理知识库（苏科版）

> **给谁**：执行方（Codex）　**谁写的**：质检方（质检方）
> **状态**：**初期调查完成 · 需求方已拍板 · 机制门已打开，可以开工**
> **日期**：2026-09-23

---

## ★ 需求方已经定的三件事（2026-09-23）

1. **教材版本：2024 新版**（不是旧版）
2. **三类新检查：全加**（单位换算 + 数量级合理性 + 图像读数）
3. **优先苏科版** —— 名称已纠正：初中物理是**苏科版**（江苏凤凰科学技术出版社），
   不是"苏教版"

## ★ 质检方已经做完的准备（你可以直接开工）

**18 个缺失单位全部补进 `UNITS` 表了**，并且登记 + 重冻了基线：

```
L  mL  mg  t  um  kPa  kJ  kW  kW·h  kWh  mA  mV  kohm  kΩ  Mohm  MΩ  dB  ℃
```

- 实测：25 个初中常用单位**全部可解析** ✓（含 `J/kg/℃`、`W/m/℃`）
- 量纲恒等自检：`L ≡ m³` ✓　`℃ ≡ K` ✓　`kW ≡ W` ✓
- **高中 21 章的 1535 项校验仍是 100%** ✓（零副作用）
- 基线：`work/physkit/quantity.py` = `7641bc82bb0225cf`

### ⚠ 两条写法约定（照做，否则会踩坑）

1. **温度一律写 `℃`，禁止用 `C`** —— 校验器把 `C` 解释成**库仑**，
   写 `20 C` 会静默当成库仑，**量纲检查一声不响地放过** ✗
2. **复合单位不要用括号** —— `J/(kg*℃)` 解析不了；
   写成连续的 `/`：**`J/kg/℃`** ✓（实测可解析）

---

## ★ 内容放哪：**同一套流水线，第二个内容源**

**不要新建一个项目去复制流水线代码。** 正确做法：

```
work/physkit/ + qc.py + build_site.py + build_lite.py + validate.py   ← 流水线（共用，不改）
        │
        ├─ work/kb/          → 高中内容（21 章，已完成）
        └─ work/kb_junior/   → 初中内容（18 章，本任务）
```

**为什么可行**：`validate.py` 和 `build_site.py` **都支持传目录参数**：

```bash
python work/validate.py              work/kb_junior    # 校验初中内容
python work/build_site.py            work/kb_junior    ../outputs/初中物理知识库.html
python work/build_lite.py            work/kb_junior    ../outputs/初中物理速查.html
```

**高中版一个字都不会被影响** —— 两个内容源互不干扰，流水线同一份。

---

## ★ 交接方式：**手动交接**（暂时不用编排器）

`work/orchestrator.py` 里的路径是**写死的**（`work/kb`、`work/批次计划.md`），
它是给高中那 21 批用的。初中版**先走手动模式**：

```
我（质检方）给要求 → 你（执行方）做一批 → 交 交接单.json + 本批报告.md
      → 我质检 → 写 质检意见.md → 你按意见改 → 通过就入库
```

好处：**不用动冻结文件**，也不用碰高中那套已经跑通的循环。
等初中版跑顺了，再考虑要不要把它也接进编排器。

---

## 〇、先说一句：这活儿**不能直接照搬高中那套**

高中版跑通了，但**初中物理的内容特性不一样**，直接开工会踩两个坑：

1. **教材有新旧两版，章节顺序完全不同**（第一节，必须需求方定）
2. **初中高频单位有 18 个校验器还不认识**（第四节，必须先开机制门）

下面逐条说。

---

## 一、★★ 必须先定：**用哪一版教材**

**"苏教版"这个说法要先纠正**：初中物理是 **苏科版**（江苏凤凰科学技术出版社）。
（"苏教版"一般指江苏教育出版社的语文、数学等。）需求方说的应该就是苏科版。

**关键问题**：苏科版初中物理**有新旧两版**，`21世纪教育网` 上标注为「苏科版(2024)」。
两版**都是 18 章**，但**章节归属和顺序不一样**：

| | 八年级上册 | 八年级下册 |
|---|---|---|
| **旧版** | 声现象 / **物态变化** / 光现象 / 光的折射 透镜 / 物体的运动 | 物质的物理属性 / **从粒子到宇宙** / 力 / 力与运动 / 压强和浮力 |
| **2024 新版** | 引言 / 声现象 / **光现象** / 光的折射 透镜 / **物态变化** / 物体的运动 | 物质的物理属性 / **力** / 力与运动 / 压强和浮力 / **从粒子到宇宙** |

另外还有两处**章节名改了**：
- 九上：旧版「电路初探」→ 新版「**简单电路**」
- 九下：旧版「电磁转换」→ 新版「**电和磁**」

### 我的建议：**用 2024 新版**

理由很实在：**需求方是在校师范生、要做教师编**，他面对的**学生现在用的是新版**。
按旧版做出来，跟现在的课堂对不上。

⚠ **但这条必须需求方亲口确认**，别替他决定。

> **版权边界**：教材目录只用来**划定范围**（哪一章讲什么），
> 所有正文、例题、常见错误**全部自己写**，不抄教材一个字。

---

## 二、范围（按 2024 新版，共 4 册 18 章）

| 册 | 章 |
|---|---|
| **八年级上册** | 引言 / 1 声现象 / 2 光现象 / 3 光的折射 透镜 / 4 物态变化 / 5 物体的运动 |
| **八年级下册** | 6 物质的物理属性 / 7 力 / 8 力与运动 / 9 压强和浮力 / 10 从粒子到宇宙 |
| **九年级上册** | 11 简单机械和功 / 12 机械能和内能 / 13 简单电路 / 14 欧姆定律 |
| **九年级下册** | 15 电功和电热 / 16 电和磁 / 17 电磁波与现代通信 / 18 能源与可持续发展 |

**建议：一批 = 一章，共 18 批。**
理由不是"整齐"，是**实测**：今天这个环境里**长回合会被网络反复掐断**
（一次 142 次重连），**短回合才跑得动**。批拆小，反而是抗断的关键。

---

## 三、★★ 可行性判断：这条要说实话

### 好消息：流水线可以**直接复用**

- 数据模型（`kb/*.json` 那套 schema）不用改 ✓
- 六类检查（量纲 / 单位 / 数值 / 方向 / 互证 / 扫描）对**公式类**内容照样适用 ✓
- 渲染出口（`build_site.py` / `build_lite.py`）不用重写 ✓
- 编排器（`orchestrator.py`）一条都不用改 ✓

### 坏消息：**初中"能自动校验"的比例，明显低于高中**

高中版能到 251 条公式、1535 项校验，靠的是**高中内容定量程度高**。
而**初中物理大量是定性的**：

| 苏科版里典型的内容 | 能不能自动校验 |
|---|---|
| 声现象（音调 / 响度 / 音色） | ✗ 定性 |
| 物态变化（六种变化、吸放热） | ✗ 以判断为主 |
| 力的概念、牛顿第一定律 | ✗ 概念性 |
| 电磁波与现代通信 / 能源 | ✗ 常识性 |
| 密度、压强、浮力、欧姆定律、电功率…… | ✓ **定量，可校验** |

**估计：全初中能写成"可校验公式"的，大约 35～50 条**（对比高中 251 条）。

→ **这不是缺陷，是学段特性**。但要提前认清：
**初中版的"校验证据密度"会薄一些，不能拿高中的数字当期望。**

### ★ 建议：给初中版**新增三类检查**，把密度补回来

初中内容有它自己"机器能判"的东西，高中版没覆盖：

| 新增检查 | 抓什么 | 例 |
|---|---|---|
| **单位换算** | 初中最常考、最容易错的地方 | `1 g/cm³ = 1000 kg/m³`、`1 kW·h = 3.6×10⁶ J`、`1 mL = 1 cm³` —— **机器判这个又快又准** |
| **数量级合理性** | 生活中的估算值不离谱 | 中学生质量约 50 kg ✓；写成 5 kg 或 500 kg 都该报错 |
| **图像读数** | v–t 图、m–V 图的斜率/面积含义 | 与高中「跨公式互证」同一路子 |

**实验步骤顺序、概念辨析**这类，**如实标「人工审核」**——
和高中版一样，**宁可承认边界，也不假装全覆盖**。

---

## 四、★★ 开工前必须先开的一道机制门：**单位表缺 18 个初中单位**

质检方已经**实测**过（把苏科版附录里的常用单位逐个喂给校验器）：

**校验器认识的**（35 个）：`m` `cm` `mm` `km` `m²` `cm²` `m³` `cm³` `s` `min` `h`
`kg` `g` `kg/m³` `g/cm³` `N` `N/kg` `Pa` `m/s` `km/h` `J` `W` `A` `V` `Ω` `C` `W·h` `Hz` `K` …

**校验器不认识的（18 个）** ✗：

```
L（升）    mL（毫升）   ℃（摄氏度）    kW·h（度）    kPa    kJ    kW
mA    mV    kΩ    kohm    mg    t（吨）    dB（分贝）    um    J/(kg*℃)    W/(m*℃)
```

**这些全是初中高频**：电能表用电度数、电流表量程、比热容单位、声强级……
**不补，初中第一章就会卡住。**

> ★★ **这是同一个坑的第二次出现。**
> 高中做第 20/21 章时也卡过一次（`eV` / `MeV` / `u` 不在表里），
> 而 `physkit/**` 是**冻结文件、执行方禁止修改** ——
> **怎么跑都写不出来，看起来像"AI 能力不行"，其实是机制没开门。**
>
> **结论**：**每换一个学段/领域开工前，先跑一遍"该领域高频单位"的可用性体检。**
> 这件事**只有质检方能做**（要改冻结文件 + 登记 + 重冻基线）。

### ⚠ 另一个必须注意的**同名陷阱**

校验器现在把 **`C` 解释成库仑**（T·I）。
但初中里 **`C` 常被当摄氏度写**（"20 C"）——
**这会静默解析成库仑，量纲检查一声不响地放过去** ✗

**要求**：温度一律写 **`℃`**（补进单位表之后），**禁止**用 `C` 表示摄氏度。

---

## 五、沿用高中的全部规矩（不变的部分）

1. **一批 = 一章**，编排器与质检流程照旧
2. **六类检查一条不少**，覆盖门槛照旧（每知识点 ≥2 数值代入、≥1 扫描、≥1 互证、≥1 条"量纲抓不到"的陷阱）
3. **常见错误必须实测**：把错误公式真跑一遍，抓不住就不算数；概念性的如实标「人工审核」
4. **冻结文件不许动**：`physkit/**` / `qc.py` / `net_guard.py` / `orchestrator.py`
   （需要加单位时**报给质检方**，由质检方登记后修改并重冻基线）
5. **一个内容源，多个出口**：初中版将来也要学生版 / 老师版，
   **不许为每个版本复制内容**

---

## 六、验收标准（可机检）

初中版每一章的成品都要满足：

- [ ] 结构检查 0 错、物理检查 100% 通过
- [ ] 每条知识点：定义 ≥30 字、物理意义 ≥60 字、≥1 场景、≥2 公式、≥3 条推导、≥2 条常见错误
- [ ] 每知识点：数值代入 ≥2、扫描 ≥1、跨公式互证 ≥1、**数值代入类陷阱 ≥1**
- [ ] 正文 / 公式名称 / 适用条件里**不许出现内部代号**（`v_avg` 这种）
- [ ] 成品单文件、**外部依赖 = 0**、窄屏不横向溢出
- [ ] **单位换算类内容必须用「单位换算检查」验证**（新增检查，见第三节）

---

## 七、开工顺序（等需求方点头之后）

1. **需求方确认用哪一版教材**（第一节）← **现在卡在这里**
2. 质检方**补 18 个单位进 `UNITS` 表** + 登记 + 重冻基线（约 10 分钟，需停机操作）
3. 质检方把 `批次计划.md` 换成初中版的 18 批
4. 执行方按批开工（一批一章）
5. 每批：执行方交 → 质检方查 → 全绿才入库

---

## 八、一句话提醒

**初中版的价值不在"公式多"，在"把最基础的东西写对、写清楚"。**
初中生最容易被"单位换算""谁的错最像对的"这类地方绊住 —— 那正好是机器最擅长验的。
**别为了凑高中的数字，硬把定性内容包装成公式。**
